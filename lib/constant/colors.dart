import 'package:flutter/material.dart';

var kColorScheme = ColorScheme.fromSeed(seedColor: Colors.purpleAccent);
var appBarTheme = const AppBarTheme().copyWith(backgroundColor: kColorScheme.primary,);
var iconColor = Colors.deepOrange;
